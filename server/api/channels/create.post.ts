// import { serverSupabaseClient } from '#supabase/server'
import type { Database } from '~/types/supabase'

export default defineEventHandler(async (_event) => {
    // TODO: Handle for when ready to implement dynamic channels.
})
